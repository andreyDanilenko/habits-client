# Мокирование авторизации для разработки

Как разрабатывать фронтенд без бекенда.

## 🎯 Что это дает

- ✅ Разработка UI без запущенного бекенда
- ✅ Тестирование всех страниц и компонентов
- ✅ Работа офлайн
- ✅ Быстрое переключение между моком и реальным API

## 🚀 Быстрый старт (30 секунд)

### Вариант 1: Через .env (рекомендуется)

1. Создайте файл `frontend/.env`:

   ```bash
   cd frontend
   echo "VITE_USE_MOCK_API=true" > .env
   ```

2. Перезапустите dev-сервер:
   ```bash
   npm run dev
   ```

### Вариант 2: Прямо в коде (для быстрого теста)

Откройте `src/shared/api/client.ts` и измените:

```typescript
// Было:
const USE_MOCK_API = import.meta.env.VITE_USE_MOCK_API === 'true' || false

// Стало:
const USE_MOCK_API = true // ← Измените на true
```

## 🎮 Использование

После включения мок-режима:

1. **Войдите с любыми учетными данными**:
   - Email: `test@example.com` (или любой другой)
   - Password: `123456` (или любой другой)
2. **Вы будете автоматически авторизованы** с моковыми данными

3. **Все страницы будут работать**:
   - ✅ Дашборд
   - ✅ Привычки (пустые списки, но UI работает)
   - ✅ Настройки
   - ✅ Профиль

## 🔄 Переключение между моком и реальным API

- **Включить мок**: `VITE_USE_MOCK_API=true` в `.env` или `USE_MOCK_API = true` в коде
- **Выключить мок**: `VITE_USE_MOCK_API=false` или `USE_MOCK_API = false` в коде

## 📋 Что работает в мок-режиме

✅ Авторизация (login/register)  
✅ Получение текущего пользователя  
✅ Логаут  
✅ Refresh токена  
✅ Защищенные маршруты (guards)  
✅ Все UI компоненты  
⚠️ Привычки, workspace - возвращают пустые массивы (можно расширить)

## 🔧 Расширение моков

Чтобы добавить моки для других данных, отредактируйте `src/shared/api/mock-client.ts`.

Например, для моковых привычек:

```typescript
if (url === '/habits') {
  return [
    {
      id: '1',
      title: 'Читать 30 минут',
      description: 'Ежедневное чтение',
      color: '#6366f1',
      userId: '1',
      workspaceId: '1',
      createdAt: new Date().toISOString(),
      updatedAt: new Date().toISOString(),
    },
  ] as T
}
```

## 🐛 Отладка

**Проблема: Авторизация не работает**

1. Проверьте, что `USE_MOCK_API = true` в `client.ts`
2. Очистите localStorage: в консоли браузера выполните `localStorage.clear()`
3. Перезагрузите страницу

**Проблема: Страница не открывается**

- Проверьте консоль браузера на ошибки
- Убедитесь, что токены начинаются с `mock-` в localStorage

## 💡 Советы

- Моковые токены начинаются с `mock-` для легкой идентификации
- Задержки имитируют реальную сеть (800ms для POST, 500ms для GET)
- Все моковые данные находятся в `mock-client.ts`

## 📝 Примечание

Мок-клиент работает на уровне API клиента, поэтому все stores и компоненты работают без изменений. Когда подключите реальный бекенд - просто выключите мок и всё заработает с реальными данными.
