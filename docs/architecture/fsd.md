# Архитектура Frontend: Feature-Sliced Design (FSD)

## Введение

Проект использует архитектуру **Feature-Sliced Design (FSD)** - методологию организации кода, которая обеспечивает масштабируемость, поддерживаемость и предсказуемость структуры проекта.

## Основные принципы

### 1. Слои (Layers)

Код организован в **7 слоев**, каждый из которых имеет свою зону ответственности:

```
app/          # Инициализация приложения
pages/        # Страницы приложения
widgets/      # Крупные самостоятельные блоки
features/     # Бизнес-функциональность
entities/     # Бизнес-сущности
shared/       # Переиспользуемые модули
```

**Правило импортов:** Слои могут импортировать только из слоев ниже себя:

- `app` → все слои
- `pages` → widgets, features, entities, shared
- `widgets` → features, entities, shared
- `features` → entities, shared
- `entities` → shared
- `shared` → ничего

### 2. Сегменты (Segments)

Каждый слой разделен на **сегменты**:

```
ui/           # Компоненты интерфейса
model/        # Бизнес-логика, стейт-менеджмент
api/          # API запросы
lib/          # Утилиты и хелперы
config/       # Конфигурация
types/        # TypeScript типы
```

**Правило:** Не все сегменты обязательны. Используйте только необходимые.

### 3. Слайсы (Slices)

Слайсы - это логические единицы внутри слоя. Например:

- `entities/user` - сущность пользователя
- `features/auth` - фича авторизации
- `widgets/header` - виджет шапки

## Структура слоев

### 📱 app/ - Инициализация приложения

**Назначение:** Инициализация приложения, настройка роутинга, глобальных провайдеров, стилей.

**Что здесь:**

- `App.vue` - корневой компонент
- `router/` - конфигурация роутинга
- `store/` - глобальные сторы (если нужны)
- `providers/` - провайдеры (темы, i18n и т.д.)
- `styles/` - глобальные стили

**Пример:**

```
app/
├── App.vue
├── router/
│   └── index.ts
└── providers/
    └── ThemeProvider.vue
```

### 📄 pages/ - Страницы приложения

**Назначение:** Композиция виджетов и фич для формирования страниц.

**Правила:**

- Один слайс = одна страница
- Страница не содержит бизнес-логику, только композицию
- Имя слайса = имя роута (например, `home`, `profile`)

**Пример:**

```
pages/
└── home/
    ├── index.ts          # Public API
    └── ui/
        └── HomePage.vue
```

**Public API:** Каждый слайс экспортирует только через `index.ts`:

```typescript
// pages/home/index.ts
export { default } from './ui/HomePage.vue'
```

### 🧩 widgets/ - Виджеты

**Назначение:** Крупные самостоятельные блоки интерфейса, композиция фич и сущностей.

**Примеры:** Header, Sidebar, ProductCard, UserProfile

**Структура:**

```
widgets/
└── header/
    ├── index.ts
    ├── ui/
    │   └── Header.vue
    └── model/
        └── useHeader.ts
```

**Правила:**

- Виджет может использовать несколько фич и сущностей
- Виджет может иметь свою локальную логику
- Виджет должен быть переиспользуемым на разных страницах

### ⚡ features/ - Фичи (Бизнес-функциональность)

**Назначение:** Конкретные действия пользователя или бизнес-сценарии.

**Примеры:**

- `auth/login` - авторизация
- `product/add-to-cart` - добавление в корзину
- `user/edit-profile` - редактирование профиля

**Структура:**

```
features/
└── auth/
    └── login/
        ├── index.ts
        ├── ui/
        │   └── LoginForm.vue
        ├── model/
        │   ├── useLogin.ts
        │   └── store.ts
        └── api/
            └── loginApi.ts
```

**Правила:**

- Фича должна быть атомарной (одна бизнес-функция)
- Фича может использовать сущности и shared
- Фича не должна зависеть от других фич напрямую

### 🏗️ entities/ - Сущности

**Назначение:** Бизнес-сущности приложения (User, Product, Order и т.д.)

**Структура:**

```
entities/
└── user/
    ├── index.ts
    ├── model/
    │   ├── types.ts
    │   ├── store.ts
    │   └── useUser.ts
    ├── ui/
    │   ├── UserCard.vue
    │   └── UserAvatar.vue
    └── api/
        └── userApi.ts
```

**Правила:**

- Сущность описывает одну бизнес-модель
- Сущность может иметь UI компоненты для отображения
- Сущность содержит типы, API, стейт-менеджмент

### 🔧 shared/ - Общие модули

**Назначение:** Переиспользуемые модули без бизнес-логики.

**Сегменты:**

```
shared/
├── ui/          # UI компоненты (Button, Input, Modal)
├── lib/         # Утилиты (formatDate, debounce)
├── api/         # Базовые API клиенты
├── config/      # Конфигурация
└── types/       # Общие типы
```

**Правила:**

- Не содержит бизнес-логики
- Максимально переиспользуем
- Независим от других слоев

## Правила работы с FSD

### ✅ Правильные импорты

```typescript
// ✅ Правильно: pages импортирует из widgets
// pages/home/ui/HomePage.vue
import { Header } from '@widgets/header'
import { LoginForm } from '@features/auth/login'

// ✅ Правильно: features импортирует из entities
// features/cart/add-to-cart/model/useAddToCart.ts
import { Product } from '@entities/product'
import { Button } from '@shared/ui'

// ❌ Неправильно: entities импортирует из features
// entities/user/model/store.ts
import { useAuth } from '@features/auth/login' // ❌
```

### ✅ Public API

Каждый слайс должен иметь `index.ts` для экспорта:

```typescript
// features/auth/login/index.ts
export { LoginForm } from './ui/LoginForm.vue'
export { useLogin } from './model/useLogin'
export type { LoginFormData } from './model/types'
```

**Импорт только через Public API:**

```typescript
// ✅ Правильно
import { LoginForm } from '@features/auth/login'

// ❌ Неправильно
import { LoginForm } from '@features/auth/login/ui/LoginForm.vue'
```

### ✅ Именование

- **Слайсы:** kebab-case (`user-profile`, `add-to-cart`)
- **Компоненты:** PascalCase (`UserCard.vue`, `LoginForm.vue`)
- **Хуки:** camelCase с префиксом `use` (`useUser`, `useAuth`)
- **Типы:** PascalCase (`User`, `ProductData`)

### ✅ Размер слайсов

- **Слайс должен быть атомарным** - одна ответственность
- Если слайс становится слишком большим (>500 строк), разбейте на под-слайсы
- Если слайс слишком маленький (<50 строк), возможно, он должен быть в shared

## Примеры организации

### Пример: Авторизация

```
features/
└── auth/
    ├── login/
    │   ├── index.ts
    │   ├── ui/
    │   │   └── LoginForm.vue
    │   ├── model/
    │   │   ├── useLogin.ts
    │   │   └── types.ts
    │   └── api/
    │       └── loginApi.ts
    └── register/
        └── ... (аналогично)
```

### Пример: Сущность User

```
entities/
└── user/
    ├── index.ts
    ├── model/
    │   ├── types.ts          # User, UserRole, UserStatus
    │   ├── store.ts          # Pinia store
    │   └── useUser.ts        # Composable
    ├── ui/
    │   ├── UserCard.vue
    │   ├── UserAvatar.vue
    │   └── UserList.vue
    └── api/
        └── userApi.ts        # CRUD операции
```

### Пример: Виджет Header

```
widgets/
└── header/
    ├── index.ts
    ├── ui/
    │   └── Header.vue
    └── model/
        └── useHeader.ts
```

Header использует:

- `@features/auth/login` - кнопка входа
- `@entities/user` - отображение пользователя
- `@shared/ui/Button` - кнопки

## Миграция на микросервисы

При переходе на микросервисы:

1. **Выделите модули** - каждая фича/сущность может стать отдельным модулем
2. **Модульная федерация** - используйте Module Federation для разделения на микросервисы
3. **Shared библиотеки** - вынесите shared в отдельные npm пакеты
4. **API Gateway** - используйте единую точку входа для API

## Чеклист для нового слайса

- [ ] Определить слой (app/pages/widgets/features/entities/shared)
- [ ] Создать директорию в правильном слое
- [ ] Создать `index.ts` для Public API
- [ ] Разделить на сегменты (ui/model/api/lib)
- [ ] Проверить импорты (только из нижних слоев)
- [ ] Экспортировать через Public API
- [ ] Добавить типы в `types.ts` (если нужны)

## Полезные ссылки

- [Feature-Sliced Design](https://feature-sliced.design/)
- [FSD для Vue](https://github.com/orgs/feature-sliced/repositories)
- [Примеры проектов](https://github.com/feature-sliced/examples)
